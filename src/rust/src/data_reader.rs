pub struct Data_reader;

impl Data_reader {
    pub fn read_data(paths: &[&str]) -> Data_reader {
        // 读取数据
        Data_reader
    }
    pub fn get_return_vectors(&self) -> &[&[f64]] {
        // 返回数据
        &[&[0.0]]
    }
}
